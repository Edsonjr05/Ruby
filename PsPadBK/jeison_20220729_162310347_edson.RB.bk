
array =[]
puts '--Leitura do Json--'

puts 'Informe o caminho do arquivo'
    arquivo = gets.chomp.to_s

file = File.open(arquivo)

file.each do |line|
     tela = line.match(/\s+_\s+/)
     if tela == nil || arr == 'SRDI06_MOD'
      else
        if !array.include?(tela)
          array.append(tela)
        end
      end

end

print array




